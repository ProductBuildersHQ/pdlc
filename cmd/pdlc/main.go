// Command pdlc inspects and evaluates a product project against the
// ProductBuildersHQ Product Development Lifecycle layout contract.
package main

import (
	"fmt"
	"os"
)

func main() {
	if err := newRootCmd().Execute(); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		os.Exit(1)
	}
}
